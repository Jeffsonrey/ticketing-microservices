$kind: http-request
url: "{{baseUrl}}/api/ticket-orders/{{ticketOrderId}}"
method: GET
scripts:
  - type: beforeRequest
    code: >-
      const id = pm.collectionVariables.get('ticketOrderId');

      if (!id) throw new Error('ticketOrderId is empty. Run Create Ticket Order
      first.');
    language: text/javascript
order: 4000
