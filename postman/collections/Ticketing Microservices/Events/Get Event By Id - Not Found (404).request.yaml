$kind: http-request
url: "{{baseUrl}}/api/events/{{missingEventId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status is 404', function () { pm.response.to.have.status(404); });
    language: text/javascript
order: 5000
