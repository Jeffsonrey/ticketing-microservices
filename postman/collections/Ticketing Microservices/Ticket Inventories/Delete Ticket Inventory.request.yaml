$kind: http-request
url: "{{baseUrl}}/api/ticket-inventories/{{ticketInventoryId}}"
method: DELETE
scripts:
  - type: beforeRequest
    code: >-
      if (pm.collectionVariables.get('runCleanup') !== 'true') {
      pm.execution.skipRequest(); }

      const id = pm.collectionVariables.get('ticketInventoryId');

      if (!id) throw new Error('ticketInventoryId is empty. Run Create Ticket
      Inventory first.');
    language: text/javascript
  - type: afterResponse
    code: pm.test('Status is 204', function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 7000
